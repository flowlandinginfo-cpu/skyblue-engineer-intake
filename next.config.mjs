/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // experimental.typedRoutes will be re-enabled once /sign-in and /sign-up
  // routes exist (next round when we port HTML preview v6 → React).
};

export default nextConfig;
